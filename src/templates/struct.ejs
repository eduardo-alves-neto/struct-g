import { IStructure } from 'strutc-g'

const structConfig: IStructure = {
  root: {
    files: [
      {
        name: 'index.tsx',
        template: 'teste.tsx.ejs',
      },
    ],
  },
}

export default structConfig
